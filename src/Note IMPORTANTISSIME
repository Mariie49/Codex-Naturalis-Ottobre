
Uno tra i metodi 			

ResourceCard visibleResourceCard = ResourceCard.drawResourceCard();
			visibleCards.add(visibleResourceCard);

			// due carte oro visibili
			GoldCard visibleGoldCard = GoldCard.drawGoldCard();
			visibleCards.add(visibleGoldCard);
			
			
 for (Corner ii : chosenCard.addCorners()) { // per ogni corner
    if (ii.getPosition() == chosenCornerPosition) { // vedo se la cornerposition è uguale a quella scelta
        chosenCorner = ii; // Se sì salvo quel corner
        break; // ed esco dal ciclo
    }
}
    		